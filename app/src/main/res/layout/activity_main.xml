<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    tools:context="com.example.naunem.secondproject.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/tvHello"
        android:drawablePadding="10dp"
        android:drawableBottom="@mipmap/ic_launcher_round"
        android:fontFeatureSettings="sans-serif"
        android:fontFamily="sans-serif"
        android:layout_gravity="right"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/edtText"
        android:layout_below="@+id/tvHello"
        android:hint="Type some text here"
        android:maxLength="10"/>

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:id="@+id/chkGender"
        android:layout_below="@+id/rgHobby"
        android:text="Male"/>

    <RadioGroup
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/rgHobby"
        android:layout_below="@+id/edtText">
        <RadioButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/rbCommic"
            android:text="Commic"
            />
        <RadioButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Novel"
            android:id="@+id/rbNovel"/>

    </RadioGroup>

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/swOn"
        android:layout_below="@+id/chkGender"/>

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/spnOrder"
        android:layout_below="@+id/swOn"
        android:entries="@array/spinner_arrays"
        android:prompt="@string/spinner.text.choose_a_position"
        android:spinnerMode="dialog">

    </Spinner>

    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/spnList"
        android:layout_below="@+id/spnOrder">

    </Spinner>

</RelativeLayout>
